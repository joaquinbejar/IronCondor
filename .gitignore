/target
/.claude
/.idea
/docs
/rules
/AGENTS.md
/CLAUDE.md
/milestones
.env
# proptest-regressions/ is committed (docs/TESTING.md §3): generated regression
# seeds are the first line of defence against a re-regression, so they are
# tracked, not ignored (issue #17 un-deferred this from #1).
*.hgrm

# Python bytecode from the pytest/bench runs (python/tests, python/benches)
__pycache__/
*.pyc
