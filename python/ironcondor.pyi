# Type stubs for the `ironcondor` extension module.
#
# Placeholder scaffold (#38): only `__version__` is exposed today. The real
# surface — `BacktestConfig`, `run`, `Bundle`, and the exception hierarchy —
# is wired by #39/#40 (docs/06 §4-§6). Shipped in the wheel so editors and
# type-checkers see the surface as it grows.

__version__: str
